import sys
from PyQt6.QtWidgets import QApplication, QMainWindow

# 🎯 ШАГ 1: СОЗДАЕМ ПУСТОЕ ОКНО
app = QApplication(sys.argv)      # 📱 ДВИГАТЕЛЬ: без этого приложение не запустится

window = QMainWindow()           # 🏠 КОРОБКА: создаем главное окно
window.setWindowTitle("Мое первое приложение")
window.setGeometry(300, 300, 400, 300)  # 📐 Размер и положение: x, y, width, height

window.show()                    # 👀 ВКЛЮЧАЕМ ВИДИМОСТЬ: делаем окно видимым

app.exec()                       # 🔄 ЗАПУСКАЕМ ДВИГАТЕЛЬ: начинаем работать